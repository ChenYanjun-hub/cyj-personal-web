# Production 环境变量 · 阿里云生产部署
# ---------------------------------------------------------------
# 用法：
#   1. 在阿里云服务器 web/ 目录下：cp .env.production.example .env.production
#   2. 把下面的 DEEPSEEK_API_KEY 换成真实生产 key（推荐和本地 dev 用不同 key 方便监控）
#   3. PM2 启动时（pm2 start ecosystem.config.js），Next.js 的 next start 会自动读这个文件
#
# 安全：
#   - .env.production 不入 git（.gitignore 已排除 .env*）
#   - 服务器上 chmod 600 .env.production 让只有 owner 能读
#   - 不在日志里 echo 这个文件内容

# DeepSeek API Key · AI 分身用
# 申请：https://platform.deepseek.com
DEEPSEEK_API_KEY=sk-your-production-key-here

# 可选 · 默认 deepseek-chat（性价比最高）
# DEEPSEEK_MODEL=deepseek-chat

# 可选 · 默认 https://api.deepseek.com
# DEEPSEEK_BASE_URL=https://api.deepseek.com

# 视觉模型 · Qwen-VL（DashScope）· HR 上传招聘 JD 图片做岗位匹配用
# 申请：阿里云百炼 https://bailian.console.aliyun.com → API-KEY（sk-...）
# 没配也不影响文字对话，只是图片匹配功能会提示未开启
DASHSCOPE_API_KEY=sk-your-dashscope-key-here
# 可选 · 默认 qwen-vl-max
# DASHSCOPE_MODEL=qwen-vl-max
# 可选 · 默认 https://dashscope.aliyuncs.com/compatible-mode/v1
# DASHSCOPE_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1

# ---------------------------------------------------------------
# 注意：以下变量已在 ecosystem.config.js 里设置，**不需要**写到这里：
#   NODE_ENV=production
#   PORT=3000
#   HOSTNAME=0.0.0.0

# 留言板 SQLite 路径 · 生产务必指向部署目录之外的持久化路径（否则每次发版会被覆盖丢数据）
BOARD_DB_PATH=/var/lib/cyjweb/board.db

# 留言板站长后台 · 邮箱+密码登录（访问 /board-admin 登录后可拖便签调位置 / 删除 / 复位）
# ADMIN_SESSION_SECRET 生成：node -e "console.log(require('crypto').randomBytes(48).toString('base64url'))"
ADMIN_EMAIL=你的登录邮箱
ADMIN_PASSWORD=你的登录密码
ADMIN_SESSION_SECRET=一串足够长的随机密钥
# 注：旧的 BOARD_ADMIN_TOKEN 已退役（删除留言改用上面的登录会话，无需再配）
