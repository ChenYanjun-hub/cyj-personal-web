# Production 环境变量 · 阿里云生产部署
# ---------------------------------------------------------------
# 用法：
#   1. 在阿里云服务器 web/ 目录下：cp .env.production.example .env.production
#   2. 把下面的 DEEPSEEK_API_KEY 换成真实生产 key（推荐和本地 dev 用不同 key 方便监控）
#   3. PM2 启动时（pm2 start ecosystem.config.js），Next.js 的 next start 会自动读这个文件
#
# 安全：
#   - .env.production 不入 git（.gitignore 已排除 .env*）
#   - 服务器上 chmod 600 .env.production 让只有 owner 能读
#   - 不在日志里 echo 这个文件内容

# DeepSeek API Key · AI 分身用
# 申请：https://platform.deepseek.com
DEEPSEEK_API_KEY=sk-your-production-key-here

# 可选 · 默认 deepseek-chat（性价比最高）
# DEEPSEEK_MODEL=deepseek-chat

# 可选 · 默认 https://api.deepseek.com
# DEEPSEEK_BASE_URL=https://api.deepseek.com

# ---------------------------------------------------------------
# 注意：以下变量已在 ecosystem.config.js 里设置，**不需要**写到这里：
#   NODE_ENV=production
#   PORT=3000
#   HOSTNAME=0.0.0.0
